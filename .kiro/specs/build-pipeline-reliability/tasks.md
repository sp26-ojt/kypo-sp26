# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Helm Release Conflict, Postgres Not Ready, SSL PKIX Failure, Insufficient Retry
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bugs exist
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate all 4 bugs exist
  - **Scoped PBT Approach**: Scope each property to the concrete failing case(s) for reproducibility
  - **Bug 1 - Helm Conflict**: Mock `helm list --all-namespaces --short` to return existing release names matching head chart releases (e.g., `crczp-head`). Assert `deploy_head_services()` succeeds without "cannot re-use a name" error. On unfixed code: FAILS because no cleanup step exists.
  - **Bug 2 - Postgres Not Ready**: Mock `kubectl get pods -n default -l app=postgres` to return pods in `Pending`/`Init:0/1` state. Assert `deploy_head_services()` waits and eventually succeeds. On unfixed code: FAILS because no readiness gate exists.
  - **Bug 3 - SSL PKIX**: Inspect `TF_VAR_users` generated by `setup_head_services_variables()`. Assert the `iss` field does NOT start with `https://`. On unfixed code: FAILS because `iss="https://$head_host/keycloak/realms/CRCZP"` is hardcoded.
  - **Bug 4 - Insufficient Retry**: Mock a heavy operation (`tofu apply`) that fails 4 consecutive times. Assert `retry_heavy()` is used and succeeds within 5 attempts. On unfixed code: FAILS because `retry()` aborts after 3 attempts and `retry_heavy()` does not exist.
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests FAIL (this is correct - it proves the bugs exist)
  - Document counterexamples found:
    - Bug 1: `deploy_head_services()` calls `tofu apply` without prior `helm uninstall` when releases exist
    - Bug 2: No `wait_for_service` call for postgres before head chart deploy
    - Bug 3: `TF_VAR_users` contains `iss="https://..."` instead of `iss="http://keycloak-service:8080/..."`
    - Bug 4: `retry()` uses `max_attempts=3`, `retry_heavy()` function does not exist in `utils.sh`
  - Mark task complete when tests are written, run, and failures are documented
  - _Requirements: 1.1, 1.2, 1.3, 1.4_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Clean VM Deploy, Postgres Already Ready, Lightweight Retry, Non-OIDC Services
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (cases where all isBugCondition functions return false)
  - **Preservation 1 - Clean VM**: Mock `helm list --all-namespaces --short` to return empty string. Observe `deploy_head_services()` proceeds directly to `tofu apply` without any cleanup step. Write property: for all env where `isBugCondition_HelmConflict(env) = false`, deploy behavior is unchanged.
  - **Preservation 2 - Postgres Already Ready**: Mock `kubectl get pods` to return all postgres pods in `Running/Ready` state. Observe deploy proceeds immediately without waiting. Write property: for all env where `isBugCondition_PostgresNotReady(env) = false`, no unnecessary delay is introduced.
  - **Preservation 3 - Lightweight Retry**: Observe `retry git clone` uses `max_attempts=3` and `delay=5`. Write property: for all op where `isBugCondition_InsufficientRetry(op) = false` (git clone, kubectl commands), `retry()` behavior is unchanged.
  - **Preservation 4 - Non-OIDC Services**: Observe services not using issuer URL are unaffected by config changes. Write property: services not referencing `TF_VAR_users` iss field continue to operate normally.
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. Fix build pipeline reliability - 4 independent bug fixes

  - [x] 3.1 Add `retry_heavy()` function to `scripts/utils.sh`
    - Add new function `retry_heavy()` after existing `retry()` function in `scripts/utils.sh`
    - Use `max_attempts=5` and `delay=30` (vs `retry()` which uses 3/5)
    - Keep identical logic to `retry()`, only parameters differ
    - Add `export -f retry_heavy` at the bottom of the file alongside other exports
    - Do NOT modify existing `retry()` function - preserve its behavior for lightweight ops
    - _Bug_Condition: isBugCondition_InsufficientRetry(op) where op.type IN {helm_install, tofu_apply} AND retry.max_attempts <= 3_
    - _Expected_Behavior: retry_heavy() uses max_attempts=5 and delay=30, covering transient failures of heavy operations_
    - _Preservation: retry() for lightweight ops (git clone, kubectl) remains unchanged with max_attempts=3, delay=5_
    - _Requirements: 2.4, 3.4, 3.5_

  - [x] 3.2 Add Helm release cleanup step in `deploy_head_services()`
    - In `scripts/03-infrastructure-deploy.sh`, inside `deploy_head_services()`, add cleanup block BEFORE the `tofu init` check
    - Check for existing Helm releases: `helm list --all-namespaces --short 2>/dev/null || true`
    - If releases exist, uninstall them: `helm list --all-namespaces -q | xargs -I{} helm uninstall {} --wait 2>/dev/null || true`
    - Log the cleanup action for observability
    - Cleanup block must be conditional (only runs when releases exist) to preserve clean VM behavior
    - _Bug_Condition: isBugCondition_HelmConflict(env) where helm list returns existing releases matching head chart names_
    - _Expected_Behavior: deploy_head_services() auto-uninstalls conflicting releases before tofu apply, deploy succeeds_
    - _Preservation: When helm list returns empty (clean VM), no cleanup step runs, deploy proceeds as before_
    - _Requirements: 2.1, 3.1_

  - [x] 3.3 Add postgres readiness gate in `deploy_head_services()`
    - In `scripts/03-infrastructure-deploy.sh`, inside `deploy_head_services()`, add `wait_for_service` call AFTER Helm cleanup and BEFORE `tofu init` check
    - Use existing `wait_for_service` utility from `utils.sh`
    - Check command: `kubectl get pods -n default -l app=postgres --field-selector=status.phase=Running | grep -c Running | grep -q '^[1-9]'`
    - Use reasonable timeout (e.g., `max_attempts=60`, `delay=10` = 10 minutes max wait)
    - _Bug_Condition: isBugCondition_PostgresNotReady(env) where postgres pods NOT all in Running/Ready state_
    - _Expected_Behavior: deploy_head_services() waits until postgres healthy before deploying head chart, head-hook-preinstall does not fail_
    - _Preservation: When postgres already ready, wait_for_service returns immediately on first check, no unnecessary delay_
    - _Requirements: 2.2, 3.2_

  - [x] 3.4 Fix issuer URL to use internal HTTP in `setup_head_services_variables()`
    - In `scripts/03-infrastructure-deploy.sh`, inside `setup_head_services_variables()`, change `TF_VAR_users` assignment
    - Replace `iss=\"https://$head_host/keycloak/realms/CRCZP\"` with `iss=\"http://keycloak-service:8080/keycloak/realms/CRCZP\"`
    - This avoids SSL certificate validation entirely by using internal Kubernetes service URL
    - No other changes to `TF_VAR_users` structure
    - _Bug_Condition: isBugCondition_SSLFailure(config) where config.issuerUrl STARTS_WITH "https://" and cert is self-signed_
    - _Expected_Behavior: TF_VAR_users uses internal HTTP URL, Java services fetch OIDC config without PKIX error_
    - _Preservation: Services not using issuer URL are unaffected; TF_VAR_openid_configuration_insecure=true remains set_
    - _Requirements: 2.3, 3.3_

  - [x] 3.5 Replace `retry` with `retry_heavy` for heavy operations in deploy functions
    - In `deploy_base_infrastructure()`: replace `retry tofu apply -auto-approve ...` with `retry_heavy tofu apply -auto-approve ...`
    - In `deploy_head_services()`: replace `retry tofu apply -auto-approve` with `retry_heavy tofu apply -auto-approve`
    - In `deploy_head_services()`: replace `retry tofu init` calls with `retry_heavy tofu init` (init can also be slow)
    - Keep `retry` (not `retry_heavy`) for: `git clone`, `kubectl` commands, and other lightweight operations
    - _Bug_Condition: isBugCondition_InsufficientRetry(op) where op.type IN {tofu_apply, tofu_init}_
    - _Expected_Behavior: Heavy ops use retry_heavy with 5 attempts/30s delay, covering transient OpenStack/Helm failures_
    - _Preservation: git clone and kubectl commands continue using retry() with 3 attempts/5s delay_
    - _Requirements: 2.4, 3.4, 3.5_

  - [x] 3.6 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - All 4 Bug Conditions Resolved
    - **IMPORTANT**: Re-run the SAME tests from task 1 - do NOT write new tests
    - The tests from task 1 encode the expected behavior for all 4 bugs
    - When these tests pass, it confirms all expected behaviors are satisfied
    - Run bug condition exploration tests from step 1
    - **EXPECTED OUTCOME**: All 4 property tests PASS (confirms all bugs are fixed)
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.7 Verify preservation tests still pass
    - **Property 2: Preservation** - No Regressions on Non-Buggy Inputs
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: All preservation tests PASS (confirms no regressions)
    - Confirm clean VM deploy, postgres-ready deploy, lightweight retry, and non-OIDC service behaviors are unchanged
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
